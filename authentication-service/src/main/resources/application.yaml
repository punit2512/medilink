spring: 
  application:
    name: authentication-service  
    
logging:
  level:
    org.springframework.security: DEBUG
    
server:
  port: 8089
#  servlet:
#    context-path: /auth

medilink:
  auth:
    db:
      url: jdbc:mysql://localhost:3306/medilink_auth
      username: medilink_auth
      password: password
      
 #Hibernate Configuration
hibernate:
  dialect: org.hibernate.dialect.H2Dialect
  hbm2ddl: 
    auto: none
  ejb: 
    naming_strategy :  org.hibernate.cfg.ImprovedNamingStrategy
  show_sql: false
  format_sql: true 
  
  
security:
  oauth2:
    resource:
      filter-order: 3